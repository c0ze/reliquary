# Direct runtime dependencies for the Reliquary MCP memory server.
#
# Pinned to exact versions: mem0 in particular makes breaking API changes
# across majors (2.0 moved the entity id from a top-level `user_id=` kwarg into
# `filters={"user_id": ...}` and renamed `limit` -> `top_k` on search()), so an
# unpinned floor silently drifts the runtime out from under the code.
#
# The Docker image installs the fully-resolved transitive set from
# requirements.lock for reproducible builds. Regenerate both after a bump:
#
#     uv pip compile requirements.txt --python-version 3.12 --no-annotate -o requirements.lock
#     uv lock
mem0ai==2.0.4
qdrant-client==1.18.0
httpx==0.28.1
uvicorn==0.48.0
PyYAML==6.0.3
# Security floor for the transitive HTTP/2 implementation (CVE-2026-71554).
h2==4.4.1
